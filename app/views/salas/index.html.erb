<p id="notice"><%= notice %></p>

<h4><%= @professor %></h4>
<h1>Salas</h1>

<table>
  <thead>
    <tr>
      <th>Descricao</th>
      <th>Escola</th>
      <th>Periodo</th>
      <th>Professor</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @salas.each do |sala| %>
      <tr>
        <td><%= sala.descricao %></td>
        <td><%= sala.escola.nome %></td>
        <td><%= sala.periodo.descricao %></td>
        <td><%= sala.professor == nil ? "Disponível" : sala.professor.nome %></td>
        <td><%= link_to 'Show', sala %></td>
        <td><%= link_to 'Edit', edit_sala_path(sala) %></td>
        <td><%= link_to 'Destroy', sala, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sala', new_sala_path %>
